# DoloPaws

## Homepage prototype testing

Use the temporary `view` query param on `/` to swap homepage variants while the real customer-state logic is still pending:

- `/` keeps the current homepage
- `/?view=new` shows the new-customer homepage prototype
- `/?view=returning` shows the returning-customer homepage prototype